import React from 'react';
import ReactLoading from 'react-loading';

export default function Loading(){
    return(
        <ReactLoading type={'spokes'} color="#357edd" height={'5%'} width={'5%'} />   
    );
};